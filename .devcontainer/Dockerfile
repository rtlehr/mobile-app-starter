# Uncomment if you need extra customization
# FROM mcr.microsoft.com/devcontainers/javascript-node:16

# Install OpenJDK and Android SDK command‑line tools, unzip, etc.
# RUN apt-get update && \
#     apt-get install -y openjdk-11-jdk unzip && \
#     mkdir -p /opt/android-sdk && cd /opt/android-sdk && \
#     curl -o sdk-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-6609375_latest.zip && \
#     unzip sdk-tools.zip && rm sdk-tools.zip

# (You can layer in any other system packages here)
